import 'package:flutter/material.dart';

class DebtorFormScreen extends StatelessWidget {
  const DebtorFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: AppBar(title: Text('Add Debtor')),
      body: Center(child: Text('Debtor form will be here.')),
    );
  }
}
